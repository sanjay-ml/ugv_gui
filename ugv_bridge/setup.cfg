[develop]
script_dir=$base/lib/ugv_bridge

[install]
install_scripts=$base/lib/ugv_bridge

[metadata]
name = ugv_bridge
version = 0.0.0

[options]
packages = find:
install_requires =
    fastapi
    uvicorn

[options.entry_points]
console_scripts =
    bridge = ugv_bridge.bridge:main
